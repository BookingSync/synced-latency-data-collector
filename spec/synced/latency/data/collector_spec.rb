RSpec.describe Synced::Latency::Data::Collector do
  it "has a version number" do
    expect(Synced::Latency::Data::Collector::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
